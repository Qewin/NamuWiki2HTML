typedef struct sstring{
	int *p; //string 자체. 
	int len; //길이.(0부터 세었다.) 
}string;
//int u2utf8(char* text[], char* end);
//int parsetable(char *text[], char* limit);
//int parsetext(char *text[], char *limit);
int parse(int Nspace, string title, string text, char* cdoc){
	//char *ctitle = u2utf8(title.p,title.end);
	//char *ctext = parsetext(text.p,text.end);
	
	return 0;
}
